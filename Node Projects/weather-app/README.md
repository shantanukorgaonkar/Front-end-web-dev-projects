# Project Details

This project a weather app using node js. 
Name of the place is passed in the CLI
Using the name the coords are obtained using func under utils folder i.e.geocode() that converts name to respt coords
Another func inside utils called forecast() uses the coords to get the current weather forecast
finally the forecast is dislayed in the cli
